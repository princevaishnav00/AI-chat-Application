name: DevSecOps End to End Pipeline

on:
    # workflow_dispatch:
    push:
        branches: [main]

jobs:

    # CI (Continuous Integration with Security Scanning)
    code-quality:
        uses: ./.github/workflows/code-quality.yml

    secrets-scan:
        uses: ./.github/workflows/secrets-scan.yml

    dependency-check:
        uses: ./.github/workflows/dependency-check.yml

    scan-dockerfile:
        uses: ./.github/workflows/scan-dockerfile.yml

    tests:
        uses: ./.github/workflows/tests.yml

    # Build once the security scans are completed.
    build:
        needs: [code-quality, secrets-scan, dependency-check, scan-dockerfile, tests]
        uses: ./.github/workflows/docker-build-push.yml
        secrets: inherit

    # Image Scan
    trivy:
        needs: [build]
        uses: ./.github/workflows/image-scan.yml
        secrets: inherit

    # Deploy to Production
    deploy:
        needs: [trivy]
        uses: ./.github/workflows/deploy-to-server.yml
        secrets: inherit
